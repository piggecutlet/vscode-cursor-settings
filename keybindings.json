// Place your key bindings in this file to override the defaults
// 既定値を上書きするには、このファイル内にキー バインドを挿入します
[
  {
    // terminal.integrated.copyOnSelection を true にする必要がある
    "key": "cmd+c",
    "command": "workbench.action.terminal.sendSequence",
    "args": { "text": "\u0003" },
    "when": "terminalFocus"
  }
]
